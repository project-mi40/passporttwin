# Copy this file to .env and fill in real values. Never commit .env itself.

# --- Database ---
POSTGRES_DB=passporttwin
POSTGRES_USER=passporttwin
POSTGRES_PASSWORD=devpassword
DATABASE_URL=postgresql://passporttwin:devpassword@postgres:5432/passporttwin

# --- Backend (FastAPI) ---
BACKEND_PORT=8000
ENVIRONMENT=development
SECRET_KEY=changeme-generate-a-real-secret-for-anything-beyond-local-dev

# --- Frontend (React/Vite) ---
FRONTEND_PORT=5173
VITE_API_BASE_URL=http://localhost:8000

# --- AI module ---
# Add model paths, thresholds, etc. as they're defined in Sprint 5+

# --- Monitoring (optional, if Grafana is used) ---
GRAFANA_PORT=3000
